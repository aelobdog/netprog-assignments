How to run the program :
   1. `make` will compile the program.
      the program can then be run as 
      `./load_balancer.c <filename> <N>`

   2. `make run` compiled and runs the
      program with the default args of
         filename=input.txt
         N=4
